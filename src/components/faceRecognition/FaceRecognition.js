import React from "react";

const FaceRecognition = ({imageURL}) => {
    return(
        <div>
            <img src={imageURL} alt="general"/>
        </div>
    );
}

export default FaceRecognition;